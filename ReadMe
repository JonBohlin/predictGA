To install, download predictGA_0.99.0.tar.gz, and type (make sure to include the path) from R:

install.packages("predictGA_0.99.0.tar.gz", repos=NULL, type="source")

Type require(predictGA) and vignette("predictGA"), ?predictGA or ?extractSites in R for help.
The package dependens on the "glmnet" library.
